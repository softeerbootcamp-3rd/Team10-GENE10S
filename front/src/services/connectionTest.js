// import axios from 'axios';

export async function connenctionTest() {
  try {
    // const response = await axios.get('/connenctionTest');
    // console.debug(response);
    console.info('hi');
  } catch (e) {
    console.error(e);
  }
}
